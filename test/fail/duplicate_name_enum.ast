enum SomeEnum {
    prefix = BT,
    values {
        one
    }
};

enum SomeEnum {
    prefix = BY,
    values {
        one
    }
};

phase RootPhase {
    passes {
        A
    }
};
pass A;

root node RootNode { attributes { int a } };
