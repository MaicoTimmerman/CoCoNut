enum SomeEnum {
    prefix = BT,
    values {
        one
    }
};

enum SomeEnum {
    prefix = BY,
    values {
        one
    }
};

root phase RootPhase {
    passes {
        A
    }
};
pass A;

root node RootNode { attributes { int a = 0 } };
