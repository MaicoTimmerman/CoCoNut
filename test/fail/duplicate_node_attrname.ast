enum SomeEnum {
    prefix = SO,
    values { a, b, c }
};

node IdExpr {
    attributes {
        int a,
        SomeEnum a
    }
};


root phase RootPhase {
    passes {
        A
    }
};
pass A;

root node RootNode { attributes { int a } };
