enum SomeOtherEnum {
    prefix = BT,
    values {
        one, two
    }
};

enum SomeEnum {
    prefix = BT,
    values {
        one, two
    }
};

phase RootPhase {
    passes {
        A
    }
};
pass A;

root node RootNode { attributes { int a } };
