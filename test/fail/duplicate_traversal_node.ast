traversal Print {
    nodes {
        IdExpr, IdExpr
    }
};

node IdExpr {
    children {
        child IdExpr somename
    }
};

phase RootPhase {
    passes {
        A
    }
};
pass A;

root node RootNode { attributes { int a } };
