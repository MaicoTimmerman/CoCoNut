enum MyEnum {
    prefix = MY,
    values {
        int, float, bool, attribute, prefix, enum, node
    }
};

node MyNode {
    attributes {
        MyEnum en,
        int a,
        float b,
        bool c
    }
};

phase RootPhase {
    passes {
        A
    }
};
pass A;

root node RootNode { attributes { int a } };
